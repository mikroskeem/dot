#!/bin/bash
DISPLAY=":0"
case ${1} in
    shrug)
        echo -n "¯\_(ツ)_/¯" | xsel -ib;;
    lenny)
        echo -n "( ͡° ͜ʖ ͡°)" | xsel -ib;;
    reich)
        echo -n "卍" | xsel -ib;;
    *)
        exit;;
esac
